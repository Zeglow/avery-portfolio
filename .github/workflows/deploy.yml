name: Build and Deploy to Vercel with Footprint Config

on:
  push:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Create config.js from secrets
      run: |
        echo "// Auto-generated config file from GitHub Secrets" > config.js
        echo "window.FOOTPRINT_CONFIG = {" >> config.js
        echo "  binId: '${{ secrets.FOOTPRINT_BIN_ID }}'," >> config.js
        echo "  apiKey: '${{ secrets.FOOTPRINT_API_KEY }}'," >> config.js
        echo "  enabled: true" >> config.js
        echo "};" >> config.js
        echo "✅ Config file created successfully"
        
    - name: Verify config file
      run: |
        if [ -f "config.js" ]; then
          echo "✅ config.js exists and ready for deployment"
          echo "First few lines of config.js:"
          head -3 config.js
        else
          echo "❌ Failed to create config.js"
          exit 1
        fi
        
    - name: Install Vercel CLI
      run: npm install --global vercel@latest
      
    - name: Pull Vercel Environment Information
      run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      
    - name: Build Project Artifacts
      run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      
    - name: Deploy Project Artifacts to Vercel
      run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
        
    - name: Deploy Summary
      run: |
        echo "🚀 Deployment completed successfully!"
        echo "📝 Config file was included in deployment"
        echo "🔒 API keys are securely managed via GitHub Secrets" 